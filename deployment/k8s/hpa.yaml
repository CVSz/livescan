apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: livescan-api
  namespace: livescan
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: livescan-api
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
