apiVersion: v1
kind: Service
metadata:
  name: livescan-api
  namespace: livescan
spec:
  selector:
    app: livescan-api
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
